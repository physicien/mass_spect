# `mass_spect`

## TO DO
- Update graph_SI_mass.py to use argparse (or merge it to graph_mass.py?).

- Add arguments for xmin, xmax and ymax + default automatic values.

- Add code support for csv input files. 
